// Site-wide toggles.
// comingSoon: set true to hide the storefront behind a maintenance splash.
// While true, visit any page with ?preview in the URL to bypass the splash
// and view the live site (e.g. https://www.naqsh-studios.com/?preview).
export const site = {
  comingSoon: true,
};
